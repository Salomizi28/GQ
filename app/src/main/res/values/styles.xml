<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:actionBarStyle">@color/ocean_blue</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="appTitle">
        <item name="android:fontFamily">@font/blocks</item>
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/orange</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="gameTitle">
        <item name="android:fontFamily">@font/cagliostro_regular</item>
        <item name="android:textSize">@dimen/secondary_title_size</item>
        <item name="android:textColor">@color/pale_orange</item>
        <item name="android:textAlignment">center</item>
        <item name="android:lineSpacingExtra">5sp</item>
    </style>

    <style name="answerButton">
        <item name="android:fontFamily">@font/cagliostro_regular</item>
        <item name="android:textSize">@dimen/default_text_size</item>
        <item name="android:textAlignment">center</item>
        <item name="android:background">@drawable/background_button_light_blue</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">4dp</item>
    </style>

</resources>
